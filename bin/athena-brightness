#!/usr/bin/env bash

brightnessctl-rs set "$1"
brightness=$(brightnessctl get)
max_brightness=$(brightnessctl max)
percent=$((brightness * 100 / max_brightness))
notify-send -h string:x-canonical-private-synchronous:brightness \
  -h int:value:$percent \
  "Brightness: ${percent}%"
